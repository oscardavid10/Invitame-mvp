<section class="container mx-auto px-5 py-12 max-w-md">
  <h2 class="text-2xl font-bold mb-4">Recuperar contraseña</h2>
  <% if (error) { %>
    <p class="mb-3 p-2 rounded bg-red-500/10 text-red-400"><%= error %></p>
  <% } %>
  <% if (ok) { %>
    <p class="mb-3 p-2 rounded bg-green-500/10 text-green-400"><%= ok %></p>
  <% } %>
  <form method="post" action="/auth/forgot" class="grid gap-3">
    <input class="rounded bg-black/30 px-3 py-2" name="email" type="email" placeholder="Email">
    <button class="btn btn-primary">Enviar</button>
  </form>
</section>
<section class="container mx-auto px-5 py-12 max-w-md">
  <h2 class="text-2xl font-bold mb-4">Entrar</h2>
  <% if (error) { %>
    <p class="mb-3 p-2 rounded bg-red-500/10 text-red-400"><%= error %></p>
  <% } %>
  <form method="post" action="/auth/login" class="grid gap-3">
    <input type="hidden" name="next" value="<%= next || '/panel' %>">
    <input class="rounded bg-black/30 px-3 py-2" name="email" type="email" placeholder="Email">
    <input class="rounded bg-black/30 px-3 py-2" name="password" type="password" placeholder="Contraseña">
    <button class="btn btn-primary">Entrar</button>
  </form>
</section>

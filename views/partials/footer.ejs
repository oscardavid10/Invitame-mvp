<%
  const anim = theme.animations?.footer || {};
  const animClass = anim.in ? `animate__animated animate__${anim.in}` : '';
  const animOutAttr = anim.out ? `data-anim-out="${anim.out}"` : '';
%>
<footer class="py-10 text-center text-[var(--muted)] <%= animClass %>" <%- animOutAttr %>>
  © <%= new Date().getFullYear() %> <%= data.title %>
</footer>
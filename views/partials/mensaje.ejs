<%
  const anim = theme.animations?.mensaje || {};
  const animClass = anim.in ? `animate__animated animate__${anim.in}` : '';
  const animOutAttr = anim.out ? `data-anim-out="${anim.out}"` : '';
%>
<section class="py-12 bg-grid/40 <%= animClass %>" <%- animOutAttr %>>
  <div class="container mx-auto px-5 max-w-3xl text-center">
    <h2 class="font-display text-3xl md:text-4xl">Nos encantará compartirlo contigo</h2>
    <p class="mt-4 text-[var(--ring)]"><%= theme.copy.intro %></p>
  </div>
</section>
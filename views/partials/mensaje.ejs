<% const anim = theme.animations?.mensaje || {}; const animClass = (anim.in || anim.out) ? `animate__animated ${anim.in?`animate__${anim.in}`:''} ${anim.out?`animate__${anim.out}`:''}` : '' %>
<section class="py-12 bg-grid/40 <%= animClass %>">
  <div class="container mx-auto px-5 max-w-3xl text-center">
    <h2 class="font-display text-3xl md:text-4xl">Nos encantará compartirlo contigo</h2>
    <p class="mt-4 text-[var(--ring)]"><%= theme.copy.intro %></p>
  </div>
</section>
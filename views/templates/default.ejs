<main>
  <%
    const order = (data.section_order
      ? JSON.parse(data.section_order)
         : ["hero","detalles","mensaje","galeria","registry","ubicacion","rsvp","footer"]);
    for (const key of order) { %>
      <%- include(`../partials/${key}`, { data, theme }) %>
  <% } %>
</main>
